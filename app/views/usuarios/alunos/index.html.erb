<div class="card card-success">
  <div class="card-header"><h2>Listagem de Alunos</h2></div>
  <div class="card-body">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Nome</th>
          <th>Email</th>
          <th>DN</th>
          <th>Responsável</th>
          <th>Contratos</th>
          <th>Agendamentos</th>
        </tr>
      </thead>
      <tbody>
        <% @alunos.each do |aluno| %>

          <tr>
            <td><%= aluno.email %></td>
            <td><%= aluno.email %></td>
            <td><%= aluno.data_nasc %></td>
            <td><%= "---" %></td>
            <td><%= aluno.contratos.count %></td>
            <td><%= aluno.agendamentos.count %></td>
          </tr>

        <% end %>
      </tbody>
    </table>
  </div>
</div>
